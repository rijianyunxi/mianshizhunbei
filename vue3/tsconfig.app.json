{
  "extends": "@vue/tsconfig/tsconfig.dom.json",  // 继承 Vue 官方的基础 DOM 配置
  "compilerOptions": {
    /* --- 核心构建配置 --- */
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo", // 指定增量编译缓存文件的位置
    "target": "ESNext",         // 编译目标为最新 JS 版本
    "module": "ESNext",         // 使用 ES Module 模块规范
    "moduleResolution": "bundler", // 【关键修改】使用适合 Vite/Webpack 的模块解析策略（解决模块解析的核心）
    "jsx": "preserve",          // 保留 JSX 语法不编译（交给 Vite 处理）
    "resolveJsonModule": true,  // 允许导入 .json 文件

    /* --- 路径别名配置 --- */
    "baseUrl": ".",             // 解析非相对模块的基准目录（配合 paths 必须设置）
    "paths": {
      "@/*": ["src/*"]          // 【路径别名】告诉 TS @ 代表 src 目录
    },


    /* --- 类型定义 --- */
    "types": [
      "vite/client"             // 引入 Vite 的客户端类型（识别 import.meta.env 等）
    ],

    /* --- 代码检查规则 (保持你原来的设置) --- */
    "strict": true,                         // 开启所有严格类型检查选项
    "noUnusedLocals": true,                 // 有未使用的局部变量时报错
    "noUnusedParameters": true,             // 有未使用的函数参数时报错
    "erasableSyntaxOnly": true,             // 仅允许使用可擦除的语法（禁止 enum 等运行时语法，Vue 推荐）
    "noFallthroughCasesInSwitch": true,     // switch 语句禁止 case 贯穿
    "noUncheckedSideEffectImports": true    // 检查未验证的副作用导入
  },

  /* --- 包含的文件 --- */
  "include": [
    "src/**/*.ts",      // 包含 src 下所有 .ts 文件
    "src/**/*.tsx",     // 包含 src 下所有 .tsx 文件
    "src/**/*.vue",     // 【必须】包含 src 下所有 .vue 文件
    "env.d.ts"
  ],

  /* --- 排除的文件 --- */
  "exclude": [
    "node_modules",     // 排除依赖包
    "dist"              // 排除打包输出目录
  ]
}