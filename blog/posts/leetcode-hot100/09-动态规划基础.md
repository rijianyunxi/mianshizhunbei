---
title: 09 - 动态规划基础（10 题）
date: 2026-03-28
description: 这一篇先把最典型的一维 DP 和网格 DP 题做扎实。
---

# 09 - 动态规划基础（10 题）

动态规划先别急着背状态转移，先问自己三件事：**状态是什么、转移从哪来、初始值是什么。**

## 81. 爬楼梯

- **标签**：动态规划

### 思路

到第 `i` 级台阶的方法数，来自第 `i-1` 级和第 `i-2` 级。

### 代码（JavaScript）

```js
function climbStairs(n) {
  if (n <= 2) return n;
  let a = 1, b = 2;
  for (let i = 3; i <= n; i++) {
    [a, b] = [b, a + b];
  }
  return b;
}
```

## 82. 杨辉三角

- **标签**：动态规划

### 思路

每一行内部元素来自上一行左上角和右上角之和。

### 代码（JavaScript）

```js
function generate(numRows) {
  const res = [];
  for (let i = 0; i < numRows; i++) {
    const row = new Array(i + 1).fill(1);
    for (let j = 1; j < i; j++) row[j] = res[i - 1][j - 1] + res[i - 1][j];
    res.push(row);
  }
  return res;
}
```

## 83. 打家劫舍

- **标签**：动态规划

### 思路

当前位置要么不抢，继承前一个；要么抢，就加上前两个位置的最优值。

### 代码（JavaScript）

```js
function rob(nums) {
  let prev2 = 0, prev1 = 0;
  for (const num of nums) {
    const cur = Math.max(prev1, prev2 + num);
    prev2 = prev1;
    prev1 = cur;
  }
  return prev1;
}
```

## 84. 完全平方数

- **标签**：动态规划

### 思路

把问题看成完全背包：枚举最后一个使用的平方数。

### 代码（JavaScript）

```js
function numSquares(n) {
  const dp = new Array(n + 1).fill(Infinity);
  dp[0] = 0;
  for (let i = 1; i <= n; i++) {
    for (let j = 1; j * j <= i; j++) {
      dp[i] = Math.min(dp[i], dp[i - j * j] + 1);
    }
  }
  return dp[n];
}
```

## 85. 零钱兑换

- **标签**：动态规划

### 思路

`dp[i]` 表示凑成金额 `i` 的最少硬币数，转移来自 `i - coin`。

### 代码（JavaScript）

```js
function coinChange(coins, amount) {
  const dp = new Array(amount + 1).fill(Infinity);
  dp[0] = 0;
  for (let i = 1; i <= amount; i++) {
    for (const coin of coins) {
      if (i >= coin) dp[i] = Math.min(dp[i], dp[i - coin] + 1);
    }
  }
  return dp[amount] === Infinity ? -1 : dp[amount];
}
```

## 86. 单词拆分

- **标签**：动态规划

### 思路

`dp[i]` 表示前 `i` 个字符能否被字典拆出，枚举最后一段。

### 代码（JavaScript）

```js
function wordBreak(s, wordDict) {
  const set = new Set(wordDict);
  const dp = new Array(s.length + 1).fill(false);
  dp[0] = true;
  for (let i = 1; i <= s.length; i++) {
    for (let j = 0; j < i; j++) {
      if (dp[j] && set.has(s.slice(j, i))) {
        dp[i] = true;
        break;
      }
    }
  }
  return dp[s.length];
}
```

## 87. 最长递增子序列

- **标签**：动态规划

### 思路

`dp[i]` 表示以 `nums[i]` 结尾的 LIS 长度，枚举前面更小的元素转移。

### 代码（JavaScript）

```js
function lengthOfLIS(nums) {
  const dp = new Array(nums.length).fill(1);
  let ans = 1;
  for (let i = 1; i < nums.length; i++) {
    for (let j = 0; j < i; j++) {
      if (nums[i] > nums[j]) dp[i] = Math.max(dp[i], dp[j] + 1);
    }
    ans = Math.max(ans, dp[i]);
  }
  return ans;
}
```

## 88. 乘积最大子数组

- **标签**：动态规划

### 思路

因为负数会翻转大小关系，所以要同时维护最大积和最小积。

### 代码（JavaScript）

```js
function maxProduct(nums) {
  let maxF = nums[0], minF = nums[0], ans = nums[0];
  for (let i = 1; i < nums.length; i++) {
    const x = nums[i];
    const candidates = [x, maxF * x, minF * x];
    maxF = Math.max(...candidates);
    minF = Math.min(...candidates);
    ans = Math.max(ans, maxF);
  }
  return ans;
}
```

## 89. 不同路径

- **标签**：动态规划

### 思路

走到当前位置的方法数等于上方方法数加左侧方法数。

### 代码（JavaScript）

```js
function uniquePaths(m, n) {
  const dp = Array.from({ length: m }, () => new Array(n).fill(1));
  for (let i = 1; i < m; i++) {
    for (let j = 1; j < n; j++) {
      dp[i][j] = dp[i - 1][j] + dp[i][j - 1];
    }
  }
  return dp[m - 1][n - 1];
}
```

## 90. 最小路径和

- **标签**：动态规划

### 思路

当前位置的最小路径和来自上边和左边较小的那个。

### 代码（JavaScript）

```js
function minPathSum(grid) {
  const m = grid.length, n = grid[0].length;
  const dp = Array.from({ length: m }, () => new Array(n).fill(0));
  dp[0][0] = grid[0][0];
  for (let i = 1; i < m; i++) dp[i][0] = dp[i - 1][0] + grid[i][0];
  for (let j = 1; j < n; j++) dp[0][j] = dp[0][j - 1] + grid[0][j];
  for (let i = 1; i < m; i++) {
    for (let j = 1; j < n; j++) {
      dp[i][j] = Math.min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j];
    }
  }
  return dp[m - 1][n - 1];
}
```

---

下一篇：[10 - 动态规划进阶与贪心（10 题）](/posts/leetcode-hot100/10-动态规划进阶与贪心)