---
title: 09 - 二分查找
date: 2026-03-28
---

# 09 - 二分查找

二分不是只能查值，很多时候是在查边界、查答案、查值域。先辨认问题的“单调性”再下手。

## 这一类题怎么识别

- 数据或答案空间存在单调性
- 题目出现“第一个/最后一个/最小满足条件”
- 可以每次排除一半搜索空间

## 这类题的通用思路

建议把“普通查找 / 左边界 / 右边界”三类模板区分清楚，别混。

## 67. 搜索插入位置

- **标签**：二分查找
- **复杂度**：时间 O(log n)，空间 O(1)

### 思路

标准找左边界模板，循环结束后的 left 就是插入位置。

### 代码（JavaScript）

```js
function searchInsert(nums, target) {
  let left = 0, right = nums.length - 1;
  while (left <= right) {
    const mid = left + ((right - left) >> 1);
    if (nums[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return left;
}
```

## 68. 搜索二维矩阵

- **标签**：二分查找
- **复杂度**：时间 O(log(mn))，空间 O(1)

### 思路

把矩阵逻辑上看成一维有序数组，按下标映射回二维坐标。

### 代码（JavaScript）

```js
function searchMatrix(matrix, target) {
  const m = matrix.length, n = matrix[0].length;
  let left = 0, right = m * n - 1;
  while (left <= right) {
    const mid = left + ((right - left) >> 1);
    const val = matrix[Math.floor(mid / n)][mid % n];
    if (val === target) return true;
    if (val < target) left = mid + 1;
    else right = mid - 1;
  }
  return false;
}
```

## 69. 在排序数组中查找元素的第一个和最后一个位置

- **标签**：二分查找
- **复杂度**：时间 O(log n)，空间 O(1)

### 思路

分别找左边界和右边界，最后组合成区间。

### 代码（JavaScript）

```js
function searchRange(nums, target) {
  function lowerBound(arr, target) {
    let l = 0, r = arr.length;
    while (l < r) {
      const mid = l + ((r - l) >> 1);
      if (arr[mid] < target) l = mid + 1;
      else r = mid;
    }
    return l;
  }
  const left = lowerBound(nums, target);
  const right = lowerBound(nums, target + 1) - 1;
  if (left >= nums.length || nums[left] !== target) return [-1, -1];
  return [left, right];
}
```

## 70. 搜索旋转排序数组

- **标签**：二分查找
- **复杂度**：时间 O(log n)，空间 O(1)

### 思路

每次判断哪一半有序，再看 target 是否落在有序半边。

### 代码（JavaScript）

```js
function search(nums, target) {
  let l = 0, r = nums.length - 1;
  while (l <= r) {
    const mid = l + ((r - l) >> 1);
    if (nums[mid] === target) return mid;
    if (nums[l] <= nums[mid]) {
      if (nums[l] <= target && target < nums[mid]) r = mid - 1;
      else l = mid + 1;
    } else {
      if (nums[mid] < target && target <= nums[r]) l = mid + 1;
      else r = mid - 1;
    }
  }
  return -1;
}
```

## 71. 寻找旋转排序数组中的最小值

- **标签**：二分查找
- **复杂度**：时间 O(log n)，空间 O(1)

### 思路

比较 `mid` 和 `right`，就能判断最小值在哪一半。

### 代码（JavaScript）

```js
function findMin(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    const mid = l + ((r - l) >> 1);
    if (nums[mid] > nums[r]) l = mid + 1;
    else r = mid;
  }
  return nums[l];
}
```

## 72. 寻找两个正序数组的中位数

- **标签**：二分查找
- **复杂度**：时间 O(log(min(m,n)))，空间 O(1)

### 思路

在较短数组上二分切分位置，使左右两边满足数量和大小条件。

### 代码（JavaScript）

```js
function findMedianSortedArrays(nums1, nums2) {
  if (nums1.length > nums2.length) return findMedianSortedArrays(nums2, nums1);
  const m = nums1.length, n = nums2.length;
  let left = 0, right = m;
  while (left <= right) {
    const i = Math.floor((left + right) / 2);
    const j = Math.floor((m + n + 1) / 2) - i;
    const maxLeftA = i === 0 ? -Infinity : nums1[i - 1];
    const minRightA = i === m ? Infinity : nums1[i];
    const maxLeftB = j === 0 ? -Infinity : nums2[j - 1];
    const minRightB = j === n ? Infinity : nums2[j];
    if (maxLeftA <= minRightB && maxLeftB <= minRightA) {
      if ((m + n) % 2 === 1) return Math.max(maxLeftA, maxLeftB);
      return (Math.max(maxLeftA, maxLeftB) + Math.min(minRightA, minRightB)) / 2;
    }
    if (maxLeftA > minRightB) right = i - 1;
    else left = i + 1;
  }
}
```

## 73. 寻找峰值

- **标签**：二分查找
- **复杂度**：时间 O(log n)，空间 O(1)

### 思路

如果 `nums[mid] < nums[mid + 1]`，说明右边一定存在峰值。

### 代码（JavaScript）

```js
function findPeakElement(nums) {
  let l = 0, r = nums.length - 1;
  while (l < r) {
    const mid = l + ((r - l) >> 1);
    if (nums[mid] < nums[mid + 1]) l = mid + 1;
    else r = mid;
  }
  return l;
}
```

## 74. 寻找重复数

- **标签**：值域二分
- **复杂度**：时间 O(n log n)，空间 O(1)

### 思路

统计数组中小于等于 mid 的数有多少，如果超过 mid，重复数就在左半区间。

### 代码（JavaScript）

```js
function findDuplicate(nums) {
  let l = 1, r = nums.length - 1;
  while (l < r) {
    const mid = l + ((r - l) >> 1);
    let count = 0;
    for (const num of nums) if (num <= mid) count++;
    if (count > mid) r = mid;
    else l = mid + 1;
  }
  return l;
}
```

---

下一篇：[10 - 动态规划基础](/posts/leetcode-hot100/10-动态规划基础)